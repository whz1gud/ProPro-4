#include <stdio.h>

int main()
{
    // char str1[10] = {'H', 'e', 'l', 'l', 'o', '\0'};
    // char str2[10] = {'H', 'e', 'l', 'l', 'o'};
    // char str3[10] = "Hello";
    // char str4[] = "Hello";

    // printf("%s, %s, %s, %s", str1, str2, str3, str4);



    // int data[6] = {1, 2 ,3, 4, 5}; // 1, 2, 3, 69, 4, 5
    // int pos = 3;
    // int size;
    
    // size = sizeof(data) / sizeof(data[0]);

    // //Istrynimas:

    // for (int i = 0; i < size; i++)
    // {
    //     printf("%d\n", data[i]);
    // }

    // printf("Size of data array: %d\n", size);

    // for (int i = pos; i < size - 1; i++)
    // {
    //     data[i] = data[i+1];    // 1, 2, 3, 4, 6, 6; i = 5
    // }
    // size--;

    // for (int i = 0; i < size; i++)
    // {
    //     printf("%d\n", data[i]);
    // }

    // //Iterpimas:

    // for (int i = size - 2; i >= pos; --i)
    // {
    //     data[i + 1] = data[i];      //i = 4; 1, 2, 3, 4, 5, 5;; i = 3; 1, 2, 3, 4, 4, 5; i = 2;;
    // }

    // data[pos] = 69;

    // for (int i = 0; i < size; i++)
    // {
    //     printf("%d\n", data[i]);
    // }

    return 0;
}